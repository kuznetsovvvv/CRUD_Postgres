# CRUD_Postgres

# Directory structure
```
.
├── main.go             //base of programm
├── go.sum              //sum of packages
├── go.mod              //dependencies
├── middleware
│   └── handlers.go     //handlers to process HTTP request
├── models
|   └── models.go       //struct json data model to add to Postgres
└── router
    └── router.go       //routing by method
```
